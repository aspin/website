import { createTheme } from "@mui/material";

const globalTheme = {
  typography: {
    fontFamily: "Courier Prime",
  },
}

const global = createTheme({
  typography: {
    fontFamily: "Courier Prime",
  },
});

export {
  global, globalTheme
}